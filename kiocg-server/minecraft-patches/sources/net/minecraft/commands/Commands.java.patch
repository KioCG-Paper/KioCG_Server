--- a/net/minecraft/commands/Commands.java
+++ b/net/minecraft/commands/Commands.java
@@ -299,6 +_,9 @@
             StopCommand.register(this.dispatcher);
             TransferCommand.register(this.dispatcher);
             WhitelistCommand.register(this.dispatcher);
+            com.kiocg.server.commands.TPSBarCommand.register(this.dispatcher); // KioCG - tps bar
+            com.kiocg.server.commands.ChunkLoadCommand.register(this.dispatcher); // KioCG - chunk load
+            com.kiocg.server.commands.TickingViewerCommand.register(this.dispatcher); // KioCG - ticking viewer
         }
 
         if (commandSelection.includeIntegrated) {
