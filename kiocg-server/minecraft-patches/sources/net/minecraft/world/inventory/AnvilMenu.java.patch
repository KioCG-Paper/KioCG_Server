--- a/net/minecraft/world/inventory/AnvilMenu.java
+++ b/net/minecraft/world/inventory/AnvilMenu.java
@@ -47,7 +_,7 @@
     private static final int SLOT_Y_PLACEMENT = 47;
     // CraftBukkit start
     public static final int DEFAULT_DENIED_COST = -1;
-    public int maximumRepairCost = 40;
+    public int maximumRepairCost = Integer.MAX_VALUE; // KioCG - EXP is money
     private org.bukkit.craftbukkit.inventory.view.CraftAnvilView bukkitEntity;
     // CraftBukkit end
     public boolean bypassEnchantmentLevelRestriction = false; // Paper - bypass anvil level restrictions
@@ -134,6 +_,7 @@
 
     @Override
     public void createResult() {
+        this.resetPlayerAbilities(); // KioCG - EXP is money: 使客户端显示消耗的等级
         ItemStack input = this.inputSlots.getItem(0);
         this.onlyRenaming = false;
         this.cost.set(1);
@@ -251,7 +_,7 @@
                 result.remove(DataComponents.CUSTOM_NAME);
             }
 
-            int finalPrice = price <= 0 ? 0 : (int)Mth.clamp(tax + price, 0L, 2147483647L);
+            int finalPrice = price <= 0 ? 0 : (int)Mth.clamp((tax + price) * 100, 0L, 2147483647L); // KioCG - EXP is money: 扣除100倍合成等级
             this.cost.set(finalPrice);
             if (price <= 0) {
                 result = ItemStack.EMPTY;
@@ -283,6 +_,8 @@
 
                 result.set(DataComponents.REPAIR_COST, baseCost);
                 EnchantmentHelper.setEnchantments(result, enchantments.toImmutable());
+
+                if (this.mayPickup(this.player, true)) this.sendFakeAbilities(); // KioCG - EXP is money: 使客户端显示消耗的等级
             }
 
             org.bukkit.craftbukkit.event.CraftEventFactory.callPrepareAnvilEvent(this.getBukkitView(), result); // CraftBukkit
@@ -327,6 +_,38 @@
     public int getCost() {
         return this.cost.get();
     }
+
+    // KioCG start - EXP is money: 使客户端显示消耗的等级
+    @Override
+    public void removed(final Player player) {
+        super.removed(player);
+        this.resetPlayerAbilities();
+    }
+
+    private boolean fakeAbilitiesSent = false;
+
+    private void sendFakeAbilities() {
+        if (!fakeAbilitiesSent && this.player instanceof ServerPlayer serverPlayer) {
+            final net.minecraft.world.entity.player.Abilities abilities = new net.minecraft.world.entity.player.Abilities();
+            abilities.apply(serverPlayer.getAbilities().pack());
+            abilities.instabuild = true;
+
+            if (serverPlayer.connection != null) {
+                serverPlayer.connection.send(new net.minecraft.network.protocol.game.ClientboundPlayerAbilitiesPacket(abilities));
+                fakeAbilitiesSent = true;
+            }
+        }
+    }
+
+    private void resetPlayerAbilities() {
+        if (fakeAbilitiesSent && this.player instanceof ServerPlayer serverPlayer) {
+            if (serverPlayer.connection != null) {
+                serverPlayer.connection.send(new net.minecraft.network.protocol.game.ClientboundPlayerAbilitiesPacket(serverPlayer.getAbilities()));
+                fakeAbilitiesSent = false;
+            }
+        }
+    }
+    // KioCG end - EXP is money: 使客户端显示消耗的等级
 
     // CraftBukkit start
     @Override
