--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -854,6 +_,7 @@
         // Paper start - entity despawn time limit
         if (this.despawnTime >= 0 && this.totalEntityAge >= this.despawnTime) {
             this.discard(org.bukkit.event.entity.EntityRemoveEvent.Cause.DESPAWN);
+            if (!this.getType().getCategory().isFriendly()) this.level().getMinecraftWorld().decreaseMobLimit(this); // KioCG - mob farm restrictor
             return;
         }
         // Paper end - entity despawn time limit
@@ -5385,4 +_,10 @@
         return ((ServerLevel) this.level()).isPositionEntityTicking(this.blockPosition());
     }
     // Paper end - Expose entity id counter
+
+    // KioCG start - chunk load
+    public boolean shouldAnalyzeLoad() {
+        return this.tickCount > 20 && this.isAlive();
+    }
+    // KioCG end - chunk load
 }
