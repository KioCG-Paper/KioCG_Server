--- a/net/minecraft/world/level/block/FarmlandBlock.java
+++ b/net/minecraft/world/level/block/FarmlandBlock.java
@@ -40,7 +_,7 @@
 
     protected FarmlandBlock(final BlockBehaviour.Properties properties) {
         super(properties);
-        this.registerDefaultState(this.stateDefinition.any().setValue(MOISTURE, 0));
+        this.registerDefaultState(this.stateDefinition.any().setValue(MOISTURE, MAX_MOISTURE - 1)); // KioCG - irrigate farmland
     }
 
     @Override
@@ -55,6 +_,7 @@
         final RandomSource random
     ) {
         if (directionToNeighbour == Direction.UP && !state.canSurvive(level, pos)) {
+            ticks.getBlockTicks().unschedule(pos, this); // KioCG - irrigate farmland
             ticks.scheduleTick(pos, this, 1);
         }
 
@@ -88,22 +_,37 @@
     protected void tick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
         if (!state.canSurvive(level, pos)) {
             turnToDirt(null, state, level, pos);
+            // KioCG start - irrigate farmland
+        } else if (state.getValue(MOISTURE) == MAX_MOISTURE) {
+            org.bukkit.craftbukkit.event.CraftEventFactory.handleMoistureChangeEvent(level, pos, state.setValue(MOISTURE, MAX_MOISTURE - 1), Block.UPDATE_CLIENTS); // CraftBukkit
+            // KioCG end - irrigate farmland
         }
     }
 
     @Override
     protected void randomTick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
         int moisture = state.getValue(MOISTURE);
-        if (moisture > 0 && level.paperConfig().tickRates.wetFarmland != 1 && (level.paperConfig().tickRates.wetFarmland < 1 || (net.minecraft.server.MinecraftServer.currentTick + pos.hashCode()) % level.paperConfig().tickRates.wetFarmland != 0)) { return; } // Paper - Configurable random tick rates for blocks
-        if (moisture == 0 && level.paperConfig().tickRates.dryFarmland != 1 && (level.paperConfig().tickRates.dryFarmland < 1 || (net.minecraft.server.MinecraftServer.currentTick + pos.hashCode()) % level.paperConfig().tickRates.dryFarmland != 0)) { return; } // Paper - Configurable random tick rates for blocks
-        if (!isNearWater(level, pos) && !level.isRainingAt(pos.above())) {
-            if (moisture > 0) {
+        // KioCG start - irrigate farmland
+        if (moisture == MAX_MOISTURE && level.paperConfig().tickRates.wetFarmland != 1 && (level.paperConfig().tickRates.wetFarmland < 1 || (net.minecraft.server.MinecraftServer.currentTick + pos.hashCode()) % level.paperConfig().tickRates.wetFarmland != 0)) { return; } // Paper - Configurable random tick rates for blocks
+        if (moisture < MAX_MOISTURE && level.paperConfig().tickRates.dryFarmland != 1 && (level.paperConfig().tickRates.dryFarmland < 1 || (net.minecraft.server.MinecraftServer.currentTick + pos.hashCode()) % level.paperConfig().tickRates.dryFarmland != 0)) { return; } // Paper - Configurable random tick rates for blocks
+        if (/*!isNearWater(level, pos) &&*/ !level.isRainingAt(pos.above())) {
+            if (moisture == MAX_MOISTURE) {
+                if (!level.getBlockTicks().hasScheduledTick(pos, this)) {
+                    level.scheduleTick(pos, this, 20 * 60 * 20);
+                }
+            } else if (moisture > 0 && !shouldMaintainFarmland(level, pos)) {
+                // KioCG end - irrigate farmland
                 org.bukkit.craftbukkit.event.CraftEventFactory.handleMoistureChangeEvent(level, pos, state.setValue(MOISTURE, moisture - 1), Block.UPDATE_CLIENTS); // CraftBukkit
             } else if (!shouldMaintainFarmland(level, pos)) {
                 turnToDirt(null, state, level, pos);
             }
         } else if (moisture < 7) {
             org.bukkit.craftbukkit.event.CraftEventFactory.handleMoistureChangeEvent(level, pos, state.setValue(MOISTURE, 7), Block.UPDATE_CLIENTS); // CraftBukkit
+            // KioCG start - irrigate farmland
+        } else {
+            level.getBlockTicks().unschedule(pos, this);
+            level.scheduleTick(pos, this, 20 * 60 * 20);
+            // KioCG end - irrigate farmland
         }
     }
 
@@ -111,9 +_,10 @@
     public void fallOn(final Level level, final BlockState state, final BlockPos pos, final Entity entity, final double fallDistance) {
         super.fallOn(level, state, pos, entity, fallDistance); // CraftBukkit - moved here as game rules / events shouldn't affect fall damage.
         if (level instanceof ServerLevel serverLevel
+                && fallDistance > 1.0 // KioCG - better mob griefing setting: 摔落大于1格才能退化耕地
             && level.getRandom().nextFloat() < fallDistance - 0.5
             && entity instanceof LivingEntity
-            && (entity instanceof Player || serverLevel.getGameRules().get(GameRules.MOB_GRIEFING))
+            && (/*entity instanceof Player ||*/ serverLevel.getGameRules().get(GameRules.MOB_GRIEFING_KIOCG)) // KioCG - better mob griefing setting
             && entity.getBbWidth() * entity.getBbWidth() * entity.getBbHeight() > 0.512F) {
             // CraftBukkit start - Interact soil
             org.bukkit.event.Cancellable cancellable;
