--- a/net/minecraft/world/level/block/TurtleEggBlock.java
+++ b/net/minecraft/world/level/block/TurtleEggBlock.java
@@ -199,6 +_,6 @@
         return !(entity instanceof Turtle)
             && !(entity instanceof Bat)
             && entity instanceof LivingEntity
-            && (entity instanceof Player || level.getGameRules().get(GameRules.MOB_GRIEFING));
+            && (/*entity instanceof Player ||*/ level.getGameRules().get(GameRules.MOB_GRIEFING_KIOCG)); // KioCG - better mob griefing setting
     }
 }
