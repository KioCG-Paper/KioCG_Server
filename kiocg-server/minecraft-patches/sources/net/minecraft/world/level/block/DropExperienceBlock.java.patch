--- a/net/minecraft/world/level/block/DropExperienceBlock.java
+++ b/net/minecraft/world/level/block/DropExperienceBlock.java
@@ -35,10 +_,31 @@
     @Override
     public int getExpDrop(BlockState state, ServerLevel level, BlockPos pos, ItemStack tool, boolean dropExperience) {
         if (dropExperience) {
-            return this.tryDropExperience(level, pos, tool, this.xpRange);
+            // KioCG start - EXP emerald ore
+            int expDrop = this.tryDropExperience(level, pos, tool, this.xpRange);
+            if (this == Blocks.EMERALD_ORE || this == Blocks.DEEPSLATE_EMERALD_ORE) {
+                final net.minecraft.world.item.enchantment.ItemEnchantments enchantments = tool.getEnchantments();
+                final net.minecraft.core.Registry<net.minecraft.world.item.enchantment.Enchantment> enchantmentRegistry = level.registryAccess().lookupOrThrow(net.minecraft.core.registries.Registries.ENCHANTMENT);
+                final int silkTouchLevel = enchantments.getLevel(enchantmentRegistry.getOrThrow(net.minecraft.world.item.enchantment.Enchantments.SILK_TOUCH));
+                if (silkTouchLevel <= 0) {
+                    final int fortuneLevel = enchantments.getLevel(enchantmentRegistry.getOrThrow(net.minecraft.world.item.enchantment.Enchantments.FORTUNE));
+                    final int count = net.minecraft.world.level.storage.loot.functions.ApplyBonusCount.OreDrops.INSTANCE.calculateNewCount(level.getRandom(), 1, fortuneLevel);
+                    expDrop += net.minecraft.util.valueproviders.UniformInt.of(this.xpRange.minInclusive() * 10 * count, this.xpRange.maxInclusive() * 10 * count).sample(level.getRandom());
+                }
+            }
+            return expDrop;
+            // KioCG end - EXP emerald ore
          }
 
         return 0;
         // CraftBukkit end
     }
+
+    // KioCG start - more enchantments
+    @Override
+    protected java.util.List<ItemStack> getDrops(final BlockState state, final net.minecraft.world.level.storage.loot.LootParams.Builder params) {
+        java.util.List<ItemStack> drops = super.getDrops(state, params);
+        return com.kiocg.world.item.enchantment.Metallurgy.onGetDrops(drops, state, params);
+    }
+    // KioCG end - more enchantments
 }
